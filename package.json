{
  "name": "nippers",
  "private": true,
  "version": "1.0.0",
  "description": "N.I.P.P.E.R.S. \u2014 after school & holiday club website with parent bookings and admin",
  "workspaces": [
    "web",
    "api"
  ],
  "scripts": {
    "dev": "cd web && netlify dev --cwd ..",
    "dev:web": "npm run dev -w web",
    "dev:azure": "swa start http://localhost:5173 --run \"npm run dev -w web\" --api-location api --api-port 7071",
    "build": "npm run build -w api && npm run build -w web",
    "test": "npm test -w api",
    "lint": "npm run lint -w web && npm run lint -w api",
    "deploy": "cd web && netlify deploy --cwd .. --build --prod",
    "deploy:azure": "bash infra/deploy.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
